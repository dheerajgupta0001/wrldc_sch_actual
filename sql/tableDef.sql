create table scada_warehouse.SCADA_SEM
(
    ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,
    TIME_STAMP DATE NOT NULL,
    SCADA_DATA NUMBER,
    SEM_DATA NUMBER,
    CONSTITUENTS_NAME VARCHAR2(50 BYTE) NOT NULL,
    UNIQUE(TIME_STAMP, CONSTITUENTS_NAME)
);